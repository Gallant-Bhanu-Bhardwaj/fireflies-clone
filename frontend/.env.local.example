# Copy this file to .env.local and fill in the values.
# .env.local is gitignored — it's your local, machine-specific config.

# Required. Base URL of the FastAPI backend (see backend/main.py).
# The app will throw at startup if this is not set.
NEXT_PUBLIC_API_URL=http://localhost:8000
